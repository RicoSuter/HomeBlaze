@using HomeBlaze.Abstractions.Devices.Light;
@using HomeBlaze.Components;
@using HomeBlaze.Components.Inputs
@using HomeBlaze.Dynamic
@using MudBlazor

@inherits ThingSetupComponent<PowerConsumersWidget>

<MudForm @bind-IsValid="_isFormValid">
    <MudTextField @bind-Value="@Thing.Width" Label="Width" Required="true" />
    <MudTextField @bind-Value="@Thing.Height" Label="Height" Required="true" />
</MudForm>

@code {
    private bool _isFormValid;

    public override bool IsValid => _isFormValid;
}